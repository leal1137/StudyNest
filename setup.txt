sudo apt update
sudo apt install nodejs npm
Restart terminal
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
Restart terminal
nvm install --lts
npm install express



If you decide to build a server using purely raw Node.js without Express, 
your code becomes much harder to read and manage.