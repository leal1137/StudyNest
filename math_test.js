// math.js

/**
 * Adderar två tal och returnerar summan.
 * @param {number} a - Det första talet.
 * @param {number} b - Det andra talet.
 * @returns {number} Summan av a och b.
 */
function add(a, b) {
  return a + b;
}

// Exportera funktionen så att testfilen kan använda den
module.exports = { add };