// api.test.js
// const request = require('supertest');
// const { app } = require('../server'); // Import your express app

describe('API Routes', () => {
    test('Placeholder test', async() => {
        expect(true).toBe(true);
    })
  
//   test('GET / should return a 200 status', async () => {
//     // Supertest simulates a GET request to your server
//     const response = await request(app).get('/');
    
//     // Check if the server responded correctly
//     expect(response.status).toBe(200);
//   });

//   // Example of testing a specific route you might have
//   test('GET /api/rooms should return JSON', async () => {
//     const response = await request(app).get('/api/rooms');
    
//     expect(response.status).toBe(200);
//     expect(response.type).toMatch(/json/);
//   });

});